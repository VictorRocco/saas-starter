from django.urls import path
from . import views

app_name = '${APP_NAME}'

urlpatterns = [
    path('', views.home, name='home'),
    path('about/', views.about, name='about'),
    path('contact/', views.contact, name='contact'),
    path('examples/bulma/', views.bulma_examples, name='bulma_examples'),
    path('examples/alpine/', views.alpine_examples, name='alpine_examples'),
    path('examples/htmx/', views.htmx_examples, name='htmx_examples'),
]